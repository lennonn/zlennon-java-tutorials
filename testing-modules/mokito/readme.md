## 例子来源：Mockito-Essentials
* dummy: 虚拟节点
* stub :存根 一个存根在其方法被调用时向调用者传递间接输入。存根仅为测试范围编程。存根可以记录其他信息，如被调用的次数等。模拟异常场景
* stub ：一个spy对象监听一个真实对象。
* mock :Mock对象是spy和stub的组合。它充当代码测试的间接输出，类似于spy，也可以stub方法以返回值或抛出异常，类似于stub。如果未调用预期方法或方法的参数不匹配，Mock对象将导致测试失败。
* fake: 伪对象是一种具有真实逻辑（不同于存根）且以某种方式更加简化或更便宜的测试替身